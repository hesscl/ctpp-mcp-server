services:
  mcp-server:
    build:
      context: ./mcp-server
    environment:
      MCP_TRANSPORT: http
      PORT: "3000"
      CTPP_API_KEY: "${CTPP_API_KEY}"
      MCP_AUTH_TOKEN: "${MCP_AUTH_TOKEN:-}"
    ports:
      - "3000:3000"
    profiles: [prod, dev]
